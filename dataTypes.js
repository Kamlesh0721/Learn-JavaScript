/*
String
Number
Bigint
Boolean
Undefined
Null
Symbol
Object
*/

// Note: null is differet from undefined - 
// If we write let str="" then it is undefined we know it is string but its value is not defined yet
// but if we write let str=null then it is null ( somethine that does not exist )

let x = 5
console.log(x);
console.log(typeof (x));

let str = "Kamlesh"
console.log(str);
console.log(typeof (str))


console.log(typeof(undefined))  

console.log(typeof(null));  // In JS typeof fxn return datatype as output. But if we write typeof(null) 
                            // It will give "object" as output some say its datatype and some people say its object
                             



